layout 구성하기
    + layout은 웹 페이지를 구성하는 요소들을 배치할 공간을 분할하고 정렬하는 것이다.
    + 공간을 분할할 때는 먼저 행을 구분한 후, 행 내부 요소를 분리하는 것이 일반적이다.

    + 웹 페이지는 style과 layout을 담당하는 CSS를 사용하는 layout을 구성한다.
    + layout의 핵심은 div와 같은 블록 엘리먼트를 원하는 위치에 배여한느 것이다.
    + CSS를 사용하여 layout을 구성할 때 자주 사용되는 핵심 기술은 float다.

    
